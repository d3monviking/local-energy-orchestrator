# Person A local dev environment (world/, gateway/, cloud/settlement, eval/).
# python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#
# Service containers (Person B's mocks/gateway/cloud/web) get their own
# per-service requirements once the Dockerfiles land.

networkx==3.7
numpy==2.4.6
pandas==2.3.3
scipy==1.18.1
PyYAML==6.0.3
requests==2.34.2

# world/feeder.py
osmnx==2.1.1
shapely==2.1.2
pandapower==3.5.5
numba==0.67.0  # JIT for pandapower's power-flow core; ~5x faster runpp_3ph solves

# world/weather.py
pyarrow==25.0.1  # parquet cache for the fetched weather series

# world/pv_truth.py
pvlib==0.15.2

# world/irt_library.py, gateway/orchestrator/planner_cvx.py
cvxpy==1.9.3

# gateway/orchestrator/irt.py
holidays==0.105  # India festival calendar for the festival-flag feature

# contracts validation, DB access for local testing
jsonschema==4.26.0
psycopg2-binary==2.9.13
